# Copy to .env and fill in your key.
# Required APIs: Maps JavaScript API, Directions API
# Restrict by HTTP referrer in Google Cloud Console.
VITE_GOOGLE_MAPS_API_KEY=your_key_here

# Optional: override daily Directions API call cap (default 150)
VITE_DIRECTIONS_DAILY_LIMIT=150